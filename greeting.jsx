/**
 * Greeting Script
 * ------------------
 * A simple JavaScript program demonstrating:
 * - Functions
 * - Variables
 * - Template literals
 * - Clean, readable code
 */

function greet(name) {
  const message = `Hello — I am your new Jr Web Developer, ${name}!`;
  return message;
}

// Example usage
const myName = "Lost Files";
console.log(greet(myName));
